# Build artifacts
dist/
*.exe

# OS-compiled binaries in repo root (from cross-compilation or
# local go-build invocations). Use glob patterns so any new cmd/
# addition is covered automatically.
/curdx
/curdx-*
/cxb-*

# Legacy binary names from the Python -> Go migration era. Kept so
# stale local builds don't get tracked if someone pulls an old branch.
/ask
/askd
/autoloop
/autonew
/cask
/cpend
/cping
/ctx-transfer
/gask
/gpend
/gping
/lask
/laskd
/lpend
/lping
/oask
/pend

# IDE
.idea/

# Built binary
/curdx

# OS
.DS_Store
Thumbs.db
.gstack/
